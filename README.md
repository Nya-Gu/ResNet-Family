# ResNet Family 구현

ResNet-50 코드를 확장해 ResNet-101, ResNet-152 모델도 학습시켜볼 수 있도록 코드를 수정했습니다.

저번과 마찬가지로 Pytorch에 구현된 것과 동일한 구조, 동일한 파라미터 수를 가지는 ResNet-50, ResNet-101, ResNet-152을 구현했습니다.

학습에 오래 걸리는 관계로 별도의 학습을 수행하지는 않았습니다.
